window:
  width: 1280
  height: 720
  title: "Wyrm"

server:
  address: "localhost:7777"
  protocol: "tcp"
  tick_rate: 20

world:
  seed: 0
  chunk_size: 512

federation:
  enabled: false
  node_id: ""           # Auto-generated from server address if empty
  peers: []             # List of peer server addresses
  gossip_interval: 5    # Seconds between gossip messages

genre: "fantasy"
